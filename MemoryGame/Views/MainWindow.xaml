<Window x:Class="MemoryGame.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MemoryGame.ViewModels"
        mc:Ignorable="d"
        Title="Memory Game" Height="550" Width="450"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Player:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding PlayerName, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="5,0" VerticalAlignment="Center"/>
            <TextBlock Text="Moves:" Margin="15,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding MoveCount}" Margin="5,0" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="Time:" Margin="15,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding GameTime}" Margin="5,0" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" Rows="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.FlipCardCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Margin="5" FontSize="32" FontWeight="Bold"
                                Background="CornflowerBlue">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Content" Value=""/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFlipped}" Value="True">
                                            <Setter Property="Content" Value="{Binding Symbol}"/>
                                            <Setter Property="Background" Value="LightGray"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMatched}" Value="True">
                                            <Setter Property="Background" Value="LightGreen"/>
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Game Completed!" FontSize="32" FontWeight="Bold"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="DarkGreen" Background="#99FFFFFF"
                       Visibility="{Binding IsGameCompleted, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="New Game" Command="{Binding NewGameCommand}" Width="120" Height="30" Margin="5"/>
            <Button Content="High Scores" Command="{Binding ShowHighScoresCommand}" Width="120" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>